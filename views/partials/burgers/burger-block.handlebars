
<div class="container">
    <div class="row">
        <div class="col-sm mt-3">
            {{burger_name}}
        </div>
        <div class="col-sm mt-3 mr-5">
            {{#unless devoured}}
            <button class="devour btn btn-primary" data-id="{{id}}" data-newsleep="{{sleep}}">
                Devour it!
            </button>
            {{/unless}}
        </div>
    </div>
    <hr>
</div>